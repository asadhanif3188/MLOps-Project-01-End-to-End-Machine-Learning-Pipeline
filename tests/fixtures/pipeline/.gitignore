# Generated by reproducing the fixture pipeline (`dvc repro`). These are the
# stage OUTPUTS: they are regenerated from the committed input (data/raw/data.csv)
# + params.yaml + dvc.yaml + src/, and their resolved hashes are pinned in the
# committed dvc.lock. Only the inputs and the lock are version-controlled; the
# outputs are reproduced, not stored.
/data/processed/
/models/
/metrics/

# Local MLflow file-store written by train/evaluate when reproduced offline
# (MLFLOW_TRACKING_URI=file:...). Never a pipeline artifact; never committed.
/mlruns/
